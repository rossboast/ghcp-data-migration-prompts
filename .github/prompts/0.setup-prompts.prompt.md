
You are the lead architect on a project that involves migrating an existing database onto a new Azure data platform. The initial target database on Azure will be CosmosDB. The project will have two key objectives: 

1. Analyse the existing database to produce documentation. This documentation should include detail on specific data feeds (tables) in the existing database and how this data is validated and transformed. Existing databases are likely to be Oracle and use PL/SQL for stored procedures, but should be open to any type of database and data feed. The database is likely to have a number of applications as consumers and you should track how applications use the data. The analysis and planning stage should account for any risks to the migration, such as identifying any logic, data or data types which can't be migrated, or will be more challenging to migrate. This documentation will effectively represent the specification for the migration.

2. Use this documentation to migrate this data store and data feeds to a new Azure data platform using PySpark on Fabric and CosmosDB. The transformation must note the validations and transformations necessary to migrate the data feeds to Azure and those that are no longer necessary. References between the documentation and the transformation scripts will be useful. The main output from this stage will be a set of PySpark scripts which can be used to migrate the data feeds to CosmosDB on Azure. You should also produce test scripts to validate that the data has been migrated correctly.

This analysis and implementation of the transformation scripts will be completed using GitHub Copilot. Produce a set of reusable GitHub prompts which can be used to implement a phased approach to producing these objectives. There should be multiple phases, a prompt per phase, and a prompt for orchestrating the user through these phases when executed. The prompts should be in a standard github structure for prompts (e.g. stored in `.github\prompts`) and should have a filename that follows github conventions for prompts (e.g. `orchestrator.prompt.md`). There should be a status manifest that keeps a track of what prompt and phase the user has executed and the main findings for each. The orchestrator prompt should guide the user through each phase in sequence, ensuring that each phase is completed before moving to the next. It may be necessary to loop back to previous phases if new information is discovered or to migrate additional data feeds.

There should be prompts for:

1. Main Orchestrator - Guide the user through each phase of the migration process, ensuring that each phase is completed before moving to the next. The orchestrator should also manage the status manifest, keeping track of what prompt and phase the user has executed and the main findings for each.
    1. Create a folder in the workspace root called '0.Delivery' which will contain all the delivery assets we create.
2. Requirements Gathering - Gather the high-level requirements for the migration project. This should include understanding the existing database structure, data feeds, and any specific requirements for the Azure data platform. This phase should also identify any constraints or limitations that may impact the migration process.
    1. Create a folder in the workspace root called '1.Requirements' under '0.Delivery' which will contain all the gathered requirements for the migration project.
    2. Prompt the user to provide high-level requirements, including top-level objectives and any specific needs for data validation, transformation, and target data structures in CosmosDB.
    3. Document any constraints or limitations that may affect the migration process, such as data types that are not supported in CosmosDB or performance considerations.
    4. Generate a 'requirements.md' file under '1.Requirements' file that captures all the high level objectives for the project work and captures all the requirements information provided directly by the user or through supplied requirements documentation. if there are structured use cases or requirements stored in user provided files, then reference these from the requirements file rather than duplicating.
3. Information Gathering - Gather existing sources of information for the database, data feeds and any consuming applications. This is essential for making a plan and for documenting the database. This phase should also include gathering any existing stored procedures, scripts, or code that is used to validate and transform the data feeds in the existing database.
    1. Create a folder in the workspace root called '2.Information_Gathering' under '0.Delivery' which will contain all the gathered information about the existing database and data feeds.
    2. Prompt the user to gather all example files, schemas, specifications, and diagrams that will support the creation of the specification for the database. Ensure the information is text-based, preferably in markdown format, to facilitate easy analysis and documentation.
    3. Add these files to the 'Key Files' section in the 'copilot-instructions.md' file plus their purpose.
    4. Add a placeholder in the copilot-instructions for each file called '<filename> processing' - this will allow the user to add any specific findings for how to understand that particular file when reading it e.g. any business context.
4. Document Database - Using the gathered information, summarise the most pertinent information from these assets. Prioritise information that describes the database and data feed from a user perspective and prioritise the information that is critical for understanding how this data is currently transformed and validated. This will be necessary to make a plan for how this database and datafeed can be migrated to Azure. This phase should also analyse the gathered information to make sure it is complete and should note any potential gaps before proceeding to the next phase.
    1. Create a folder in the workspace root called '3.Database_Documentation' under '0.Delivery' which will contain all the documentation about the existing database and data feeds. An existing database will have multiple data feeds (tables) so create subfolders for each data feed under this folder.
5. Plan Migration - Using the documentation produced in the previous phase, produce a detailed plan for how the data can be migrated to the Azure platform using CosmosDB as a target data source. This plan should include details on how each data feed will be migrated, including any necessary transformations and validations. The plan should also identify any risks or challenges associated with the migration and propose mitigation strategies. This phase should also include a mapping of existing data types and structures to their equivalents in CosmosDB.
    1. Create a folder in the workspace root called '4.Migration_Plan' under '0.Delivery' which will contain all the planning documents for migrating the data feeds to Azure CosmosDB. Each artifact should be stored in its own subfolder for clarity.
    2. Explore what approaches we have to implement the objectives and complete the migration. Perform analysis of the requirements and the documentation to identify the best approach for the migration. 
    3. Based on this analysis, inform the user of the options and trade-offs for each approach. Feel free to ask the user for more information if this helps to narrow down the options.
    4. Break down the implementation plan into smaller tasks and goals, however, keep the whole plan and technical design in mind to reduce the risk of rework later.
    5. Use the migration tracker to track which tasks have been completed and which are still outstanding. It should be possible to update the status of each task as it is completed, and add additional tasks as needed.
6. Implement Migration - Using the documentation, produce PySpark scripts and accompanying documents on how the data can be migrated to the Azure platform using CosmosDB as a target data source. These scripts must implement consistent validation and transformation. The scripts should be modular and reusable for migrating additional data feeds in the future. This phase should also include test scripts to validate that the data has been migrated correctly. Finally, any data transformations should be consistent with how existing applications consume the data.
    1. Create a folder in the workspace root called '5.Migration_Implementation' under '0.Delivery' which will contain all the migration scripts and test scripts for migrating the data feeds to Azure CosmosDB. Each artifact should be stored in its own subfolder for clarity.
7. Review and Iterate - Review the outputs from the previous phases, ensuring that the documentation is complete and that the migration scripts function as expected. This phase should include testing the migration process with sample data to identify any issues or gaps. If any issues are found, the user should be guided to revisit previous phases to address these issues before finalizing the migration process.
    1. Create a folder in the workspace root called '6.Review_and_Test' under '0.Delivery' which will contain all the review notes, test results, and any revised documentation or scripts.

Each prompt should include clear instructions for the user on what information to provide and what outputs are expected.